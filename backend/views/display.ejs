<!DOCTYPE html>
<html>

<head>
    <title>NextServe</title>
    <meta http-equiv="refresh" content="5" />

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <style>
        body {
            background: radial-gradient(circle at center, #1a1a2e 0%, #0f0f1d 100%);
            color: white;
            font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .stylish-card {
            background: rgba(10, 10, 30, 0.4);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.18);
            border-radius: 20px;
            box-shadow: 0 8px 32px 0 rgba(0, 0, 0, 0.5);
            transition: all 0.3s ease-in-out;
            overflow: hidden;
            position: relative;
            padding: 0;
        }
        
        @keyframes rotating-border {
            from {
                transform: rotate(0deg);
            }
            to {
                transform: rotate(360deg);
            }
        }
        
        .moving-border-shine::before {
            content: "";
            position: absolute;
            top: -100%;
            left: -100%;
            width: 300%;
            height: 300%;
            background: conic-gradient( transparent, #ff00ea, #00d0ff, transparent);
            z-index: 0;
            animation: rotating-border 6s linear infinite;
            pointer-events: none;
        }
        
        .moving-border-shine .card-content {
            position: relative;
            z-index: 1;
            background: rgba(10, 10, 30, 0.7);
            border-radius: 20px;
            margin: 1px;
            padding: 3rem;
        }
        
        .serving-number {
            font-size: 10rem !important;
            font-weight: 600;
            color: #00ffaa;
            text-shadow: 0 0 20px #ff0026, 0 0 30px rgba(0, 255, 170, 0.5);
            letter-spacing: 5px;
            line-height: 1;
        }
        
        @media (max-width: 768px) {
            .serving-number {
                font-size: 6rem !important;
            }
            .moving-border-shine .card-content {
                padding: 2rem;
            }
        }
    </style>
</head>

<body class="d-flex justify-content-center align-items-center vh-100">
    <div class="text-center" style="max-width: 90%; width: 500px">
        <h1 class="mb-5 fw-light text-uppercase" style="letter-spacing: 4px">
            Now Serving
        </h1>

        <div class="stylish-card moving-border-shine">
            <div class="card-content">
                <h2 class="serving-number display-1">
                    <%= queue.currentServing || "---" %>
                </h2>
            </div>
        </div>
    </div>
</body>

</html>